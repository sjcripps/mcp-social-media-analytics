startCommand:
  type: http
configSchema:
  type: object
  required:
    - apiKey
  properties:
    apiKey:
      type: string
      title: "API Key"
      description: "Your EzBiz Services API key. Get a free key at https://social.ezbizservices.com"
      x-from: header
exampleConfig:
  apiKey: "your-api-key-here"
